package com.normsoule.pixelblitz.utils{	import flash.display.Sprite;	import flash.text.TextField;    import flash.text.TextFieldAutoSize;    import flash.text.TextFormat;	import flash.utils.Timer;	import flash.events.Event;	import flash.events.TimerEvent;		/**	 * The FPSCounter class creates a simple, aproximate, frames per second counter.	 * There are much more accurate fps counters available, 	 * but this one is good to get an idea of where the framerate is going.	 */	public class FPSCounter extends Sprite	{		private var ticks:int;		private var FPS:TextField;		private var format:TextFormat = new TextFormat();		private var t:Timer = new Timer( 1000 );				/**		 * Creates a FPSCounter instance and positions it based on the supplied values.		 * @param x The x position in pixels.		 * @param y The y position in pixels.		 */		public function FPSCounter( x:int = 0, y:int = 0 )		{			this.x = x;			this.y = y;						createText();						t.addEventListener( TimerEvent.TIMER, updateFPS );			addEventListener( Event.ENTER_FRAME,onEnterFrame );			t.start();		}		/**		 * @private		 */		private function createText():void 		{			format.font = "Verdana";			format.size = 9;						FPS = new TextField();			FPS.defaultTextFormat = format;			FPS.autoSize = TextFieldAutoSize.LEFT;			FPS.textColor = 0xFFFFFF;			FPS.background = true;			FPS.backgroundColor = 0x666666;			FPS.antiAliasType = "advanced";			FPS.selectable = false;			FPS.text = "FPS: ";					addChild( FPS );		}		/**		 * @private		 */		private function updateFPS( event:TimerEvent ):void 		{			FPS.text = "FPS: "+ ticks;			ticks = 0;		}		/**		 * @private		 */		private function onEnterFrame( event:Event ):void		{			ticks ++;					}	}}